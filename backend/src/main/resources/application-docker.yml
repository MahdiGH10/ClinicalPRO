spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:clinicpro_db}
    username: ${DB_USERNAME:clinicpro_user}
    password: ${DB_PASSWORD:clinicpro_pass}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}

server:
  port: 8080
